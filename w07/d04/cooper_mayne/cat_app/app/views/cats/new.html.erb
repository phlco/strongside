<h1>New cat</h1>

<%= form_for @cat do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :tagline %>
  <%= f.text_field :tagline %>

  <%= f.label :img_url %>
  <%= f.text_field :img_url %>
  <%= f.submit %>

<% end %>

<%= link_to 'Back', cats_path %>
