<!DOCTYPE html>
<html>
<head>
  <title>HerMajestysSecretApp</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
    <link href="//netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap.min.css" rel="stylesheet">

      <script>
    //  NOTE: http://stackoverflow.com/questions/7514922/rails-with-underscore-js-templates
    _.templateSettings = {
        interpolate: /\{\{\=(.+?)\}\}/g,
        evaluate: /\{\{(.+?)\}\}/g
    };
  </script>

</head>
<body>

<div class="container">
  <h1>Bond Films Info</h1>
  <hr />
  <div class="page"></div>
</div>

  <script type="text/template" id="film-list-template">
     <a href="#" id="all" class="btn btn-primary">All Bonds</a>
     <a href="#" id="tot" class="btn btn-primary">Total Gross</a>
     <a href="#" id="str" class="btn btn-primary">Star Count</a>
     <a href="#" id="lon" class="btn btn-primary">Loneliest Bond</a>
     <a href="#" id="odd" class="btn btn-primary">Odd Bonds</a>
     <a href="#" id="bst" class="btn btn-primary">Best Bond</a>
     <a href="#" id="wst" class="btn btn-primary">Worst Bond</a>
     <hr />
     <div id="info-area"></div>
     <table class="table striped">
       <thead>
         <tr>
           <th>Year</th><th>Title</th><th>Actor</th><th>Gross</th><th></th>
         </tr>
       </thead>
       <tbody>
         {{ _.each(films, function(film) { }}
           <tr>
             <td>{{= htmlEncode(film.get('year')) }}</td>
             <td>{{= htmlEncode(film.get('title')) }}</td>
             <td>{{= htmlEncode(film.get('actor')) }}</td>
             <td>{{= htmlEncode(film.get('gross')) }}</td>
           </tr>
         {{ }); }}
       </tbody>
     </table>
   </script>

<%= yield %>

</body>
</html>
