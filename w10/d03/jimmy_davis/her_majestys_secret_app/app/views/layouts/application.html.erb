<!DOCTYPE html>
<html>
<head>
  <title>HerMajestysSecretApp</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
    <link href="//netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap.min.css" rel="stylesheet">

      <script>

    _.templateSettings = {
        interpolate: /\{\{\=(.+?)\}\}/g,
        evaluate: /\{\{(.+?)\}\}/g
    };
  </script>

</head>
<body>

<div class="container">
  <h1>Bond Films Info</h1>
  <hr />
  <div class="page"></div>
</div>

  <script type="text/template" id="film-list-template">
     <a href="#" id="get_actors" class="btn btn-primary">Get Actors</a>
     <a href="#" id="total_gross" class="btn btn-primary">Total Gross</a>
     <a href="#" id="star_count" class="btn btn-primary">Star Count</a>
     <a href="#" id="loneliest_bond" class="btn btn-primary">Loneliest Bond</a>
     <a href="#" id="odd_bonds" class="btn btn-primary">Odd Bonds</a>
     <a href="#" id="best_bond" class="btn btn-primary">Best Bond</a>
     <a href="#" id="worst_bond" class="btn btn-primary">Worst Bond</a>
     <hr />
     <div id="show-info"></div>
     <table class="table striped">
       <thead>
         <tr>
           <th>Year</th>
           <th>Title</th>
           <th>Actor</th>
           <th>Gross</th>
           <th></th>
         </tr>
       </thead>
       <tbody>
         {{ _.each(films, function(film) { }}
           <tr>
             <td>{{= htmlEncode(film.get('year')) }}</td>
             <td>{{= htmlEncode(film.get('title')) }}</td>
             <td>{{= htmlEncode(film.get('actor')) }}</td>
             <td>{{= htmlEncode(film.get('gross')) }}</td>
           </tr>
         {{ }); }}
       </tbody>
     </table>
   </script>

<%= yield %>

</body>
</html>
